package;
import modchart.*;
import flixel.math.FlxAngle;

// just so its not all cluttering up PlayState
//aqui empieza la travesia -Ed
class ModchartShit {
  public static function setupModchart(state:PlayState, modManager:ModManager, songName:String){
    switch(songName){
      case 'perdition':
        //lmao
    }
  }
}

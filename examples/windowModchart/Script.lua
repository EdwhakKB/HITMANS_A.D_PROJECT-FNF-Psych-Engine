function onCreatePost()
----ESTACHANDA----

---alpha
setWindowsTransparency(win,alpha)
easeWindowsTransparency(win,alpha,time,ease)

---modifiers
--[[
winModifier

shake
Xmove
YMove
Zmove/scala
bump
warp

]]--
setWindowModifier(name,winmodifier,win)
addWindow(x,y,z,visibility)
setWinSpecificMod(win,value)

--TOPSECRET--
setWindowSkew(x,y,z,v,b)
startWarpWin(object,win)

---fps
--120 = 60
--60 = 30
--infi = 200
--infi low = 30


------------------
end